# PortfolioWebsite

## Project structure

- `index.html` contains the portfolio page markup.
- `styles/styles.css` holds the visual theme, glass effects, and responsive styling.
- `scripts/` contains the JavaScript behavior, including dark mode toggling and dynamic UI polish.
 
